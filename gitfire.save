#!/bin/bash
#step one is to read your commit message that you want to sent to version control
read your_commit_message

#next we add the contents of your folder (.) to the staging area
git add .

#next we add the commit with your message in quotes
git commit -m "$your_commit_message"

#finally we push this commit to github or whatever remote repe you have configured
git push
